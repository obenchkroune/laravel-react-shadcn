# laravel + react + shadcn-ui

This is a simple starter template to build applications using laravel and typescript react (inertia)

This is simply a [laravel/breeze](https://github.com/laravel/breeze) react + typescript template tweaked to use shadcn ui (tailwindcss + radix ui) in the frontend.

